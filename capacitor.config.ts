import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.licence3.NIM',
  appName: 'NIM',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
